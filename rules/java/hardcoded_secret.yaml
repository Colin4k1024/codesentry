id: JAVA_HARDCODED_SECRET
name: Hardcoded Secret
description: Detects hardcoded passwords, tokens, API keys in Java
severity: SEVERE
category: security
languages:
  - java
suggestion: Use environment variables or a secrets manager
patterns:
  - type: regex
    pattern: '(?i)(password|token|api_key|secret|apiKey|secretKey)\s*[=:]?\s*["\x27"][^"\x27]{8,}["\x27]'
    comment: Possible hardcoded secret
  - type: regex
    pattern: '(?i)"[a-f0-9]{32,}"'
    comment: Possible hardcoded secret (long hex string)
  - type: regex
    pattern: '(?i)"[A-Za-z0-9_/+=]{40,}[+/=]{2}"'
    comment: Possible hardcoded secret (base64-like string with padding)
